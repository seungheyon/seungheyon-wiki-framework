# seungheyon-wiki 운영 스키마

이 프로젝트는 이승현이라는 사람에 대한 모든 지식을 축적·관리하는 개인 위키다.
설계 패턴은 [`reference/llm-wiki.md`](reference/llm-wiki.md)를 따른다 — raw(원본) / wiki(합성 페이지) / schema(이 문서) 3계층 구조로,
새 소스를 넣을 때마다 새로 처음부터 찾는 게 아니라 **위키 페이지 자체를 갱신**해서 지식이 누적되게 한다.

Obsidian에서 열람하는 것을 전제로 만든다. `[[위키링크]]` 문법과 YAML frontmatter를 항상 사용한다.

---

## 1. 3계층 구조

```
seungheyon-wiki/
├── CLAUDE.md          # 이 문서 — 스키마
├── index.md           # 최상위 인덱스 (도메인 카탈로그)
├── log.md             # 전체 위키 append-only 로그
├── reference/          # 위키 자체에 대한 참고문서 (llm-wiki.md 등)
└── <도메인>/            # 예: career, projects. 사람의 다른 삶의 영역이 생기면 형제 폴더로 추가
    ├── index.md         # 도메인 인덱스
    ├── raw/              # 불변 원본 소스 — LLM은 읽기만 하고 절대 수정하지 않는다
    ├── people/           # 인물 허브 페이지 (해당 도메인에 실제로 필요할 때만)
    ├── concepts/         # 개념/사건/사물 페이지 (프로젝트, 지표, 시스템, 의사결정 등)
    ├── companies/        # (career 전용) 기업별 페이지
    └── interviews/       # (career 전용) 면접 시험지·답안·오답노트
```

**도메인 확장 규칙**: 지금은 `career/`(취업 준비), `projects/`(사이드 프로젝트 아이디에이션·설계·의사결정),
`blog/`(블로그 원본·문체 표본), `self-model/`(회고로 알아낸 자기 지식) 네 도메인이 채워져 있다.
건강, 인간관계, 자기계발 등 다른 삶의 영역에 대한 소스가 생기면 같은 층위에 새 도메인 폴더를 만들고, 루트 `index.md`에 등록한다.
미리 빈 폴더를 만들어두지 않는다 — 실제 소스가 생겼을 때 만든다. `people/`·`companies/` 등 하위 폴더도 그 도메인에 실제로
필요한 것만 만든다 — 예: `projects/`는 `people/`·`companies/` 없이 `raw/` + `concepts/`만 사용한다.

**`career/interviews/` 규칙 (career 전용)**: 이력서 항목 단위 면접 훈련 산출물을 두는 곳으로,
[[.claude/skills/interview-drill/SKILL|interview-drill]] 스킬이 생성·갱신한다. 하위 폴더는
**이력서 버전**으로 나눈다(`career/interviews/260501/`처럼 이력서 파일명의 버전을 그대로 딴다) —
이력서 문장이 바뀌면 나올 질문도 바뀌므로 어느 서술 기준의 시험지였는지가 경로에 남아야 하기 때문이다.
각 버전 폴더 안에 `질문지_<프로젝트>.md`(1개) · `답안_<프로젝트>_<YYYYMMDD>.md`(응시 회차마다 새로) ·
`오답노트_<프로젝트>.md`(1개, 계속 갱신)를 둔다. 이력서 원본은 `career/raw/이력서/`에 그대로 두고
**절대 수정하지 않으며**, 연결은 각 문서 frontmatter의 `resume:` 위키링크로만 한다. 버전을 가로지르는
추적(같은 프로젝트의 회차별 오답 추이)은 `career/interviews/index.md` 대시보드가 담당한다.
이 규칙은 career 도메인 한정이며 다른 도메인으로 확장하지 않는다.

**`self-model` 도메인 전용 규칙**: 회고로 알아낸 "이 사람이 어떤 사람인가"를 누적하는 도메인이다.
커리어 나침반(Compass Companion) 프로젝트가 RAG로 하려던 역할을 위키의 갱신 구조로 대체한 것이며,
전환 사유와 실측 근거는 `self-model/index.md`에 있다. 핵심은 **추가(RAG)가 아니라 갱신**이라는 점이다
— 새 근거가 들어오면 문서가 하나 더 생기는 게 아니라 기존 서술이 고쳐진다.

저장 단위는 **성질(trait)**이다. 성향·가치관·강약점·판단기준을 축으로 나누어 문서를 쪼개지 않는다.
하나의 성질이 여러 축에 걸치기 때문이다(중립성은 성향이자 가치관이자 강점이자 다른 약점의 원인이자
판단기준이다). 축은 문서 분할 기준이 아니라 성질에 붙는 표식으로만 쓴다. 문서는 세 갈래로 나뉜다 —
**경험은 `raw/`**(회고 원본, 불변), **성질은 `concepts/모델.md`**(계속 갱신), **궤적은 `concepts/이력.md`**
(성질 ID를 달아 append). 회고를 요약해 다시 쓰는 별도 "경험 문서"는 만들지 않는다.
`daily-feedback` 스킬의 회차 기록은 `self-model/피드백/`에 둔다 — raw가 아니다. raw는 외부에서 온
불변 스냅샷이고 피드백은 이 위키가 만든 산출물이라 전제가 틀린 것으로 밝혀지면 정정할 수 있어야 한다.

새 근거가 기존 서술을 **지지**하면 근거만 추가하고, **신규**면 항목을 추가하고, **충돌**하면
AskUserQuestion으로 사용자에게 확인한 뒤 서술을 고친다. 충돌 판정은 "기존 서술로 행동을 예측했을 때
새 근거가 그 예측을 빗나가게 하는가"이며, 표현 강도나 뉘앙스 차이는 충돌이 아니다. 물어서 정했다면
그 답변 자체가 새 근거이므로 raw나 이력의 근거 칸에 남긴다. 성질 하나는 80줄, 모델 파일 전체는
1,000줄을 상한으로 하고 넘으면 성질별 파일로 분할한다. 갱신 절차와 상세 규칙은
[[.claude/skills/self-model-ingest/SKILL|self-model-ingest]] 스킬이 갖고 있으며, 모델 문서를 직접 고치지 않고
이 스킬을 통해 갱신한다. 재료는 회고에 한정되지 않는다 — 탐구 세션, 본인이 쓴 정리 등 자신에 대한
맥락이면 무엇이든 같은 절차로 반영한다.

회고 원본은 노션에서 계속 작성하고 위키로 편입한다(raw = 외부 스냅샷이라는 기존 원칙 그대로).
파일명은 `회고_YYMMDD.md`로 **노션 페이지 제목의 날짜**를 쓴다 — `created_time`은 일괄 이관 탓에
전부 같은 날짜라 쓸 수 없다는 것이 2026-08-13 실측으로 확인됐다.

`career/concepts/자기인식_약점클러스터`는 이 도메인으로 옮기지 않는다. 얕은 탐구 위에 면접용 서술이
덧입혀져 있어, 사실 대조가 필요할 때만 참조한다. 두 페이지가 같은 주제를 다르게 서술하면
`self-model` 쪽이 대표 버전이다.

**`projects` 도메인 전용 규칙**: 각 프로젝트는 `projects/concepts/프로젝트_<이름>.md`로 관리한다(career 도메인의
프로젝트 페이지와 동일한 명명 관례를 재사용 — 새 `type` 값을 만들지 않고 기존 `concept` 유형을 그대로 쓴다). 설계 논의·의사결정은
`projects/concepts/decisions/결정_<프로젝트>_<주제>_<YYYYMMDD>.md`로 남기고(허브 페이지와 분리된 하위 폴더 —
프로젝트마다 결정이 여러 건 쌓여 가시성이 떨어지는 것을 막기 위함) 프로젝트 허브 페이지의 "설계 로그" 섹션에서
링크한다(career의 decision-journal과 동일 패턴, 저장 위치만 다름). 허브 페이지(`concepts/`)에서 결정 페이지
(`concepts/decisions/`)로 나가는 링크는 `[[decisions/결정_...]]`, 결정 페이지에서 허브로 돌아오는 링크는
`[[../프로젝트_...]]`로 상대경로를 붙인다(career 도메인이 폴더를 넘나들 때 쓰는 관례와 동일). 실제 코드/개발은
별도 저장소에서 진행하며, 이 위키에는 설계·요구사항·의사결정만 남긴다.

프로젝트가 실제 구현 단계로 넘어가면, 허브 페이지에 "구현 저장소" 섹션을 추가해 코드가 있는 별도 저장소 경로를
기록한다. 구현 작업(디자인/개발/배포)을 맡을 스킬은 **그 프로젝트 전용으로**(예: `family-archive-designer`,
`family-archive-developer`, `family-archive-devops`) 새로 만든다 — 여러 프로젝트가 공유하는 범용 구현 스킬을
미리 만들어두지 않는다(project-kickoff/project-log 같은 위키 기록용 스킬과 달리, 구현 스킬은 프로젝트마다
실제 코드베이스·기술 스택이 달라 범용화하면 내용이 공허해진다 — 다음 프로젝트가 실제로 생기면 그때 판단).
스킬 폴더명/`name` 필드는 **영어 프로젝트 이름**을 접두사로 쓴다(구현 저장소 이름과 동일하게, 예: `family-archive-*`
— 위키 페이지 제목 `프로젝트_가족아카이브`처럼 한글일 필요 없음). 이 접두사 규칙은 앞으로 만드는 모든 프로젝트
전용 구현 스킬에 동일하게 적용한다. `.claude/skills/<프로젝트>/<역할>/SKILL.md`처럼 중첩 폴더로 만들지 않는다
— Claude Code의 스킬 탐색은 `.claude/skills/<name>/SKILL.md` 단일 레벨만 인식하며, 중첩 폴더는 스킬로 전혀
인식되지 않는다(2026-07-29 `Skill` 도구로 직접 테스트해 확인됨 — 세 가지 이름 표기를 모두 시도했으나 전부
"Unknown skill" 에러). 항상 평탄한 접두사 구조(`<프로젝트>-<역할>/`)로 만든다.
이 스킬들은 wiki 세션에서 실행되지만 코드는 그 구현 저장소에만 작성한다 — 공통 안전수칙(cwd 관리, git 정책,
비밀정보 취급)은 `.claude/skills/_shared/implementation-conventions.md`를 공유해서 참조한다. 구현 저장소의
git 커밋·푸시는 wiki와 달리 자동화하지 않고 매 작업 단위 종료 시 사용자 확인을 받는다(저장소별로 별도 승인
필요, wiki의 자동 승인이 확장되지 않음).

---

## 2. raw 레이어 원칙

- raw는 **불변**이다. LLM은 raw 파일을 절대 수정하지 않는다. 원본이 갱신되면(예: 이력서 새 버전) 새 파일로 추가하고 기존 파일은 그대로 둔다.
- raw는 **출처(source of truth)**이지 읽는 대상이 아니다. 사람이나 LLM이 지식을 조회할 때는 wiki 레이어(people/concepts/companies)를 먼저 본다.
- **다른 프로젝트(employment 등)와의 중복은 신경 쓰지 않는다.** raw는 그 프로젝트에서 복사해온 스냅샷이며, 원본이 이동/삭제돼도 이 위키는 영향받지 않도록 자기완결적으로 유지한다.
- raw 내부에서의 중복(같은 사실이 여러 파일에 흩어진 경우)은 **wiki 레이어에서 캐논 페이지 하나로 해소**한다 — raw 파일들은 지우거나 합치지 않고 그대로 두되, wiki 페이지가 "이 사실의 대표 버전"이 되고 raw 파일들은 `sources:`로만 링크된다.

---

## 3. wiki 페이지 컨벤션

### frontmatter (모든 wiki 레이어 페이지 공통)

```yaml
---
type: person | concept | company | index
domain: career
tags: [태그1, 태그2]
updated: YYYY-MM-DD
sources:
  - "[[raw/경로/파일명]]"
status: null | 진행중 | 종료 | 대기   # concept/company에서만 의미 있을 때 사용
---
```

### 위키링크

- 위키 내부 페이지 참조는 항상 `[[페이지명]]` (확장자 없이, Obsidian 컨벤션).
- raw 원본을 가리킬 때도 `[[career/raw/companies/IKC/report_IKC_260508]]`처럼 위키링크로 건다 (Obsidian이 raw 폴더도 볼트 안에 있는 한 정상 작동).
- 페이지 본문 안에 최소 하나 이상의 관련 페이지로 나가는 링크와, 가능하면 들어오는 링크(다른 페이지에서 이 페이지를 언급)가 있도록 한다 — 고아 페이지 방지.

### 페이지 유형별 구조

- **person**: 기본정보 → 목표 → 강점 → 약점/자기인식 → 핵심 프로젝트(링크) → 성과지표(링크) → 관련 회사(링크)
- **concept**: 정의/요약 → 세부 내용 → 등장 맥락(어느 회사 자소서/인터뷰에 어떻게 쓰였는지) → 출처
- **company**: 기업 개요 → 지원 직무 → 파이프라인 상태(현재 단계, 날짜) → 준비 과정에서 나온 산출물(자소서/면접 등, 링크) → 이 회사 지원을 통해 발견한 것(회고 있으면)

---

## 4. index.md / log.md 운영

- **index.md** (루트, 각 도메인): 카테고리별로 페이지를 나열, 각 항목에 한 줄 요약. 새 페이지가 생기거나 페이지 성격이 바뀔 때마다 갱신한다.
- **log.md** (루트): append-only. 항목 형식은 `## [YYYY-MM-DD] ingest|query|lint | 제목` 으로 통일해서 `grep "^## \["`로 최근 이력을 추적할 수 있게 한다.

---

## 5. 워크플로우

**Ingest** (새 소스 추가): raw에 원본을 추가 → 핵심 내용 파악 → 관련된 기존 wiki 페이지들(people/concepts/companies)을 갱신 → 새로 필요한 페이지가 있으면 생성 → 도메인 index.md와 루트 log.md 갱신.

**Query** (질문 답변): 먼저 관련 도메인의 index.md를 읽고 관련 페이지를 찾는다 → 페이지를 읽고 답한다 → 답변 중 새로운 종합/비교가 나왔고 재사용 가치가 있으면 wiki 페이지로 남길지 사용자에게 제안한다.

**Lint** (주기적 점검): 페이지 간 모순, 오래된 주장(새 소스가 갱신했는데 안 반영된 것), 고아 페이지, 언급은 되지만 자기 페이지가 없는 개념, 빠진 상호링크를 점검한다.

---

## 6. 방법론/템플릿 문서에 대한 방침

기업분석·인터뷰·자소서 작성 등의 **재사용 가능한 방법론 템플릿**(예: employment 프로젝트의 `.claude/skills/A1~H1`)은
"이승현에 대한 지식"이 아니라 프로세스 도구이므로 이 위키의 raw로 복사하지 않는다.
필요하면 관련 페이지에서 원본 프로젝트 경로를 텍스트로 언급하는 정도로만 참조한다.
단, 실제 개인 데이터가 섞인 문서(예: G1 지원동기 탐구 기록, 기업별 실전 산출물)는 raw로 포함한다.

---

## 7. 글로벌 운영 규칙 (이 레포 한정 · 반드시 준수)

이 세 규칙은 훅이 아니라 이 문서 + 레포 커밋 파일로 강제한다. `git clone` 시 그대로 따라오며 machine-global 설정을 만들지 않는다.

1. **작업 완료 후 필수 `git commit & push`** — 파일을 변경한(=실제 작업이 끝난) 턴에는 durable 승인 하에 자동으로 `git add` → `commit` → `push`(origin main)한다. 단순 질문답변/탐색만 한 턴은 예외. 커밋 메시지는 무엇을 왜 바꿨는지 한 줄 요약.
2. **셀프 임프루빙 (강한 강제)** — 사용자가 피드백을 주거나 회고 중 내 실수를 발견하면 **즉시 `.claude/LESSONS.md`에 `- [YYYY-MM-DD] 교훈 / 왜 / 적용` 형식으로 append**하고 두 번 다시 반복하지 않는다. **매 세션 시작 시 `.claude/LESSONS.md`를 반드시 읽고** 준수한다.
3. **모호하면 AskUserQuestion** — 요구사항·범위·방식에 조금이라도 모호함이 있으면 추측하지 말고 반드시 AskUserQuestion으로 물어본다.
4. **`git pull` 후 변경사항 요약·점검 필수** — 원격에서 pull 받은 직후에는 항상 새로 들어온 커밋들을 확인하고(예: `git log <이전 HEAD>..<새 HEAD>`), 무엇이 왜 바뀌었는지 요약한다. 특히 CLAUDE.md·`.claude/` 등 운영 규칙 파일 자체가 바뀌었으면 내용을 읽고 사용자에게 짚어준다. 충돌(conflict)이나 예상 밖의 변경(권한 변경, 삭제된 파일 등)이 보이면 즉시 사용자에게 보고한다.
5. **근거보다 센 주장을 하지 않는다 (비약·과장 금지)** — 설명을 매끄럽게 만들려고 주장의 세기를 올리지 않는다. 원인을 하나로 단정하거나, 이점을 실제보다 크게 말하거나, 여러 이유 중 하나를 "진짜 이유"로 승격시키지 않는다. 특히 **제약·한계를 근거로 들 때는 실제 상한값과 현재값을 확인한 뒤에만** 쓴다("~때문에 불가능/위험하다"는 대개 "특정 경로가 막혔다"가 정확하다). 확인하지 못했으면 **"미확인"이라고 표시**하고, 근거가 여럿이면 무게 순으로 적되 검증된 것과 추정을 구분한다. 이 규칙은 블로그·이력서 같은 산출물뿐 아니라 **대화 답변·결정 페이지·커밋 메시지 등 모든 발화**에 적용된다.

## 8. 스킬 카탈로그 / 트리거

스킬은 `.claude/skills/`에 있다. 자연어로 아래 트리거를 말하면 해당 스킬이 로딩된다.

| 스킬 | 유형 | 대표 트리거 |
|---|---|---|
| `skill-creator` | 생성기(범용) | "스킬 만들어줘", "새 스킬 생성" |
| `wiki-ingest` | ingest | "위키화해줘", "위키에 넣어/반영/정리해줘", "새 소스 추가" |
| `wiki-query` | query | "위키에서 찾아/알려줘", "위키 기준으로 ~" |
| `wiki-lint` | lint | "위키 점검/린트/건강검진", "모순 찾아줘" |
| `interview-prep` | 특수 query | "<회사> 면접 준비/대비", "예상질문 뽑아줘" |
| `interview-drill` | 특수(질문지·오답추적) | "이력서로 질문지 만들어줘", "시험지 만들어줘", "질문지 응시할래", "오답노트 정리해줘" |
| `decision-journal` | 특수 ingest | "이 결정 기록해줘", "오퍼 정리해줘", "방향 정했어" |
| `backend-resume` | 특수(산출물) | "이력서 써줘/고쳐줘", "이력서에 반영해줘", "이력서 피드백 줄게" |
| `project-kickoff` | 특수 ingest | "<프로젝트> 시작할래", "새 프로젝트 아이디어 있어", "MVP 정의하자" |
| `project-log` | 특수 ingest | "<프로젝트>에 이 논의/결정 기록해줘", "설계 바뀐 거 반영해줘" |
| `blog-post` | 특수(오케스트레이션) | "블로그 글 써줘", "이 소스로 블로그 초안 만들어줘" |
| `blog-writer` | 특수(작성) | "초안 써줘", "이 내용으로 블로그 글 써줘" (보통 `blog-post`가 호출) |
| `blog-reviewer` | 특수(검토) | 직접 호출하지 않음 — `blog-post`가 Agent 툴로 격리된 서브에이전트에서 실행 |
| `blog-extract-style` | 특수 ingest | "문체 학습시켜줘", "스타일 프로필 갱신해줘" |
| `family-archive-designer` | 프로젝트 전용 구현 | "가족아카이브 화면/UI 만들어줘", "가족아카이브 디자인하자" |
| `family-archive-developer` | 프로젝트 전용 구현 | "가족아카이브 개발/기능 구현해줘" |
| `family-archive-devops` | 프로젝트 전용 구현 | "가족아카이브 배포하자", "인프라/CI-CD 설정해줘" |
| `book-open-interaction` | 프로젝트 전용 구현(모션) | "앨범 열리는 애니메이션", "책 넘기는 인터랙션", "실물 앨범처럼 동작하게" |
| `commerce-order-service-rules` | 프로젝트 전용 작업규칙 | "이커머스 주문 서비스", "주문 서비스 M1/M2 구현", "실전 과제 ADR" |
| `self-model-ingest` | 특수 ingest | "모델 갱신해줘", "이거 모델에 반영해줘", "회고 넣어줘", "나에 대해 알아낸 거 정리해줘" |
| `daily-feedback` | 특수(피드백) | "피드백 줘", "오늘 피드백", "태스크 피드백해줘", "지금 상황 봐줘" |
| `linkedin-post` | 유틸리티(자동화) | "링크드인에 올려줘", "이 내용으로 링크드인 포스팅해줘" |
| `oss-contribution` | 유틸리티(외부 기여) | "오픈소스 기여할 만한 거 찾아줘", "이 이슈 기여할 만한지 검토해줘" |

새 스킬은 `skill-creator`로 만들고 이 표에 한 줄 추가한다. 면접 관련 스킬은 트리거가 겹치기 쉬우니
(`interview-drill` = 이력서 문장 검증, `interview-prep` = 기업별 예상질문) 새로 만들 때 문구를 대조한다. 프로젝트 전용 구현 스킬(디자이너/개발자/데브옵스)은
프로젝트마다 새로 만든다 — 위 3개는 가족아카이브 전용이며 다른 프로젝트에 재사용하지 않는다.
