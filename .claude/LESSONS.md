# LESSONS — 셀프 임프루빙 로그

seungheyon-wiki 하네스의 자기교정 기록. 레포에 커밋되어 클론에 따라온다.

**규칙(강제):**
- 사용자가 피드백을 주거나, 회고 중 내 실수를 발견하면 **즉시** 아래에 append 하고 두 번 다시 반복하지 않는다.
- **매 세션 시작 시 이 파일을 반드시 읽고** 그 교훈을 준수한다.

형식: `- [YYYY-MM-DD] 교훈 / 왜: … / 적용: …`

## 교훈
- [2026-07-28] 새 원격/레포에서는 첫 커밋 전에 push 권한을 확인한다 / 왜: 첫 push가 권한 없는 origin으로 403 실패 / 적용: 작업 시작 시 `git remote -v` + 쓰기 권한 점검.
- [2026-07-28] 범위·방식·대상이 조금이라도 모호하면 추측하지 말고 AskUserQuestion 먼저 / 왜: 사용자 명시 글로벌 규칙 #3 / 적용: 모든 세션에서 상시 적용.
- [2026-07-28] AskUserQuestion 등 도구 인자(JSON)에 literal `\u` 유니코드 이스케이프를 절대 넣지 않는다 / 왜: 한글을 `\u...`로 적었다가 JSON 파싱 실패로 호출 1회 실패 / 적용: 인자에는 실제 문자(한글 그대로)만 사용.
- [2026-07-28] 사용자 답변에 등장한 한글 단어(특히 명사)를 그대로 재사용할 때, 다음 턴에 다시 타이핑하지 말고 원문을 복사해서 쓴다 / 왜: "앨범"을 이후 질문에서 "앱범"/"압범"/"앞범"으로 3번 연속 오타내 사용자가 "그게 뭐냐"고 되물음 — 오타를 스스로 감지하지 못한 채 반복함 / 적용: 사용자가 도입한 용어는 그대로 복붙하거나, 확신이 없으면 재입력 전에 원문 대조.
- [2026-07-28] 기존 규칙이 특정 도메인/스코프(예: `projects` 전용)에 한정돼 있을 때, "일관성 있어 보인다"는 이유만으로 다른 도메인(`career` 등)까지 임의로 확장하지 않는다 — 실제 인스턴스가 아직 하나도 없는 도메인에 미리 규칙을 넓히는 것도 포함 / 왜: 사용자가 "결정 페이지를 decisions/ 폴더로 관리하자"고 한 요청을 projects 전용으로 이미 처리된 걸 보고도 career까지 도메인 공통 규칙으로 혼자 일반화했다가, 사용자가 "그건 projects 한정이었고 career까지 확장할 생각 없었다"고 정정함 — §7-3(모호하면 AskUserQuestion)이 이미 있는 규칙인데도 스코프 확장이라는 걸 인지하지 못해 못 지킴 / 적용: 스키마·규칙 파일(CLAUDE.md, 스킬)을 고칠 때 적용 범위(도메인/스킬 전체 vs 특정 하나)가 명시돼 있지 않으면, 넓히기 전에 반드시 AskUserQuestion으로 스코프를 확인한다.
- [2026-07-29] 새 스킬을 만들 때 "다른 유사 스킬이 범용이니까 이것도 범용으로"라는 유추만으로 이름·경로·내용을 일반화하지 않는다 — 사용자가 명시적으로 "이 프로젝트 전용"이라는 맥락(예: "이 설계 문서에 따라")으로 요청했으면 그 하나의 구체 대상 전용으로 만든다 / 왜: 바로 위 교훈(2026-07-28, decisions/ 폴더 career 확장 실수)과 **같은 세션 안에서 정확히 같은 패턴을 반복**함 — `project-kickoff`/`project-log`(위키 기록용, 프로젝트가 달라도 절차가 안 바뀜)와 `가족아카이브-designer/developer/devops`(실제 코드를 다루는 구현 스킬, 프로젝트마다 스택·컨벤션이 완전히 다름)를 같은 성격으로 오인해 `project-designer` 같은 범용 이름·내용으로 3개나 만든 뒤 사용자가 지적해서 전부 다시 만듦 / 적용: 스킬을 새로 만들 때마다 "이게 위키 기록 절차(범용이어도 내용이 안 바뀜)인지, 실제 코드/산출물을 다루는 구현 스킬(프로젝트마다 내용이 달라짐)인지"부터 구분한다. 후자는 기본값을 프로젝트 전용으로 잡고, 사용자가 명시적으로 "범용으로 만들어줘"라고 할 때만 일반화한다. 같은 교훈을 이미 한 번 어겼다면, 지금 하려는 일이 그 패턴과 같은 모양인지 스스로 재점검하는 습관이 필요하다 — "교훈을 기록했다"와 "그 교훈을 다음 판단에 실제로 적용했다"는 다르다.
- [2026-07-29] 사용자가 "A인지 확인해보고 나서 다시 얘기해줘"처럼 확인→보고를 명시적으로 요청하면, 확인한 결과를 바탕으로 곧장 실행하지 말고 **먼저 결과를 보고하고 사용자의 다음 지시를 기다린다** / 왜: 스킬 폴더 구조(중첩 vs 평탄)를 "확인해보고 다시 얘기해달라"는 요청을 받고도, 확인(간접적 추론 수준)한 뒤 그 결과로 곧바로 평탄 구조를 선택해 스킬을 만들어버림 — 사용자는 "얘기해달라"고 했지 "확인되면 알아서 진행해라"라고 하지 않았는데 이 차이를 무시함 / 적용: "확인 후 얘기해줘/알려줘/보고해줘" 류의 요청은 정보 전달까지가 임무이고, 그다음 행동은 별도 지시를 받은 뒤에 한다. 단, 확인 자체는 확실한 근거(직접 실행 테스트 등)로 하고 간접 추론만으로 결론짓지 않는다.
- [2026-07-29] 대화 전체가 한국어로 진행 중일 때, 응답 중 일부만 갑자기 영어로 쓰지 않는다 / 왜: 도구 사용 사이의 텍스트 응답 하나를 영어로 썼다가, 사용자가 이를 실행 로그의 일부로 오인해서 그 안에 있던 실제 판단(스킬 폴더 구조 확정)을 못 보고 넘어감 — 언어가 갑자기 바뀌면 사람이 "이건 시스템 출력이지 나에게 하는 말이 아니다"라고 착각하기 쉽다는 걸 실제로 겪음 / 적용: 세션의 대화 언어(여기서는 한국어)를 문단 단위로도 절대 이탈하지 않는다. 코드/커맨드/고유명사 등 불가피한 영어 조각은 괜찮지만, 판단·설명 문단 전체가 영어로 새어나가지 않도록 매 응답 전에 언어를 확인한다.
- [2026-07-29] "스킬 하나만 원한다"는 요청을 받으면, 코드/저장소/배포가 딸린 "사이드 프로젝트"로 자동 승격시키지 않는다 / 왜: 링크드인 자동 발행을 "개인 세션 자동화 기능"으로 요청받았는데, `projects` 도메인의 project-kickoff(별도 구현 저장소가 생기는 사이드 프로젝트용) 흐름을 먼저 제안해 사용자가 "프로젝트 허브까지 만들 필요가 있나? 내가 할 일을 자동화하는 '기능'을 원하는 거야"라고 정정함 — 이 레포에 이미 있는 blog-post류(별도 저장소 없이 `.claude/skills/`에 단독 파일로 존재하는 유틸리티 스킬) 선례를 먼저 대조했어야 함 / 적용: 새 자동화 요청이 오면 "결과물이 별도 코드 저장소로 남아야 하는 프로젝트인지" vs "이 위키 세션에서 반복 실행할 기능인지"부터 구분하고, 후자면 project-kickoff 없이 곧장 `.claude/skills/<이름>/SKILL.md`로 만든다.
- [2026-07-29] "확인해달라"는 요청 뒤에 이어지는 선택형 질문(AskUserQuestion 등)에 사용자가 답했다고 해서, 그걸 "빌드 진행 승인"으로 확대 해석하지 않는다 / 왜: 사용자가 애초에 "스킬을 직접 만들기 전에 가능한지 확인해달라"고 명시했는데, 확인·설계 논의 이후 "토큰 발급부터 할지 스킬 베이스부터 할지" 같은 **진행 순서를 고르는 질문**에 사용자가 답한 것만으로 실제 스킬 파일 생성까지 진행함 — 사용자는 발행 목적/취지를 먼저 나와 동기화하고 싶어서 "확인"까지만 원했던 것이었고, 결국 다 만든 스킬을 지워달라고 요청받음. 바로 위(2026-07-29, decisions/ 폴더 확장 실수 뒤 project-designer 반복 실수)와 이번 건 모두 "이미 기록된 교훈—확인 후엔 보고까지만, 다음 행동은 별도 지시 후—을 같은 세션 안에서 다시 어긴" 사례 / 적용: 다단계 작업에서 "진행 순서"나 "방식"을 고르는 질문은 승인 질문이 아니다. 실제 파일 생성·발행 등 되돌리기 번거로운 행동 직전에는 "이제 진행해도 될까요?"처럼 행동 자체에 대한 명시적 승인을 별도로 받는다.
- [2026-07-29] LinkedIn `LinkedIn-Version` 헤더처럼 "특정 시점의 최신값"을 하드코딩한 값은 시간이 지나면 만료된다는 걸 전제하고, 실패 시 최신 값을 프로빙해서 스킬 파일 자체를 갱신한다 / 왜: `linkedin-post` 스킬에 박혀 있던 `LinkedIn-Version: 202401`이 만료돼 발행 시도가 `426 NONEXISTENT_VERSION`으로 실패함 — 여러 버전(202410~202506)을 순차 프로빙해 활성 버전(202503)을 찾아 발행에 성공했고, 이 교훈 직후 SKILL.md의 하드코딩 값도 최신으로 갱신함 / 적용: 외부 API 버전·날짜처럼 "작성 시점 기준 최신"으로 박아둔 값이 있는 스킬은 실패 시 값 자체가 stale해졌을 가능성을 먼저 의심하고, 재시도 전에 실제 유효 값을 탐색해 스킬 파일을 함께 고친다. Windows git-bash 환경에서 `jq`가 없을 수 있으니 JSON 바디 구성은 `python3` + Windows 경로(`cygpath -w`)로 대체하고, POSIX 경로(`/c/...`)를 네이티브 Windows `python3`에 직접 넘기면 파일을 못 찾는다는 것도 함께 기록.
- [2026-07-31] `wrangler dev`(로컬)의 R2 바인딩은 로컬 시뮬레이션 스토리지라 실제 프로덕션 R2 객체에 접근하지 못한다 — Supabase 같은 외부 DB는 `.dev.vars`가 실제 클라우드 인스턴스를 가리키면 로컬/프로덕션이 같은 데이터를 공유하지만, R2는 그렇지 않다 / 왜: family-archive의 `content_hash` 백필 라우트를 로컬 `wrangler dev`로 테스트했더니 실제로는 존재하는 R2 원본 3개를 전부 "missing"으로 오판(로컬 R2가 비어있어서) — 실제 프로덕션 R2 상태와 무관하게 로컬에서는 항상 이렇게 나온다는 걸 뒤늦게 깨달음 / 적용: R2(또는 다른 로컬/원격이 분리되는 바인딩)에 의존하는 기능은 로컬 `wrangler dev`로 "검증됐다"고 말하지 않는다 — DB 로직 검증까지만 로컬로 하고, R2 실물 접근이 필요한 부분은 배포 후 실제 사이트에 대고 별도로 확인한다.
- [2026-07-31] 특정 스킬 하나의 출력 품질 이슈(질문 누락, 톤 과장 등)는 LESSONS.md에 "기억해 두기"로 남기지 않고, 해당 스킬의 SKILL.md나 그 스킬이 읽는 참고 문서(스타일 프로필 등)를 직접 고친다 / 왜: blog-writer 초안 리뷰 중 출처 링크 누락·자기비판 톤 과장 두 가지를 처음엔 LESSONS.md에 적었는데, 사용자가 "이건 위키 전체 규칙이 아니라 blog-writer 하나의 개선 아니냐"고 지적함 — LESSONS.md 기존 항목은 전부 세션 전반의 행동 규칙(git 권한, AskUserQuestion, 유니코드 이스케이프 등)이고, 스킬 하나의 산출물 품질 문제를 여기 섞으면 스코프가 흐려짐 / 적용: "이 실수가 어떤 작업을 하든 반복될 위험(세션/에이전트 행동)"이면 LESSONS.md, "특정 스킬의 산출물 품질 문제"면 그 스킬의 SKILL.md(절차)나 참조 문서(스타일 프로필 등, 내용)를 직접 수정한다 — 전자만 여기 남긴다.
- [2026-08-01] 세션이 여러 날에 걸쳐 이어질 때(사용자가 며칠 뒤 대화를 재개하는 경우), log.md/LESSONS.md 등에 날짜를 적기 전에 `date +%Y-%m-%d`로 실제 시스템 날짜를 확인한다 — 세션 시작 시점 날짜를 기억에 의존해 계속 재사용하지 않는다 / 왜: 세션 초반(2026-07-29)에 쓴 날짜를 그 뒤로도 계속 복사해 썼는데, 실제로는 사용자가 며칠 뒤(2026-08-01) 대화를 재개했고 그 사이 로그 항목 2개가 실제 작업일보다 사흘 이른 날짜로 잘못 기록됨 — git 커밋 타임스탬프(자동 기록이라 정확)와 log.md 항목 날짜(내가 수동 타이핑)를 대조해서야 발견함 / 적용: 대화가 오래 이어지거나 시간이 흐른 것 같으면(사용자의 재개 멘트, 긴 공백 등) 날짜를 적기 전에 `date` 명령으로 재확인한다. 이미 커밋된 항목의 날짜가 틀렸다면 append-only 원칙상 항목 자체를 새로 만들지 않고, 헤더의 날짜만 직접 정정한다(내용 삭제가 아니라 메타데이터 오타 수정이므로).
- [2026-08-03] Windows에서 `wrangler dev`/`opennextjs-cloudflare build`가 잔여 `node.exe`/`workerd.exe` 프로세스 때문에 `.open-next` 삭제 `EPERM`이나 포트 충돌로 실패하면, bash `taskkill //F //IM workerd.exe`부터 시도하지 말고 안 잡히면 바로 PowerShell `Get-Process node,workerd | Select Id,ProcessName` → `Stop-Process -Id ... -Force`로 넘어간다 / 왜: git-bash의 `taskkill` 출력이 한글 로캘 코드페이지 문제로 매번 깨져 나오고("찾을 수 없습니다" 등이 깨진 바이트로 표시), 실제로 프로세스가 남아있어도 `taskkill //F //IM workerd.exe`가 못 찾는 척 실패하는 경우가 있었음(family-archive 홈 화면 버튼 정렬 작업 중 `.open-next` EPERM으로 재확인) — PowerShell의 `Get-Process`/`Stop-Process`는 실제 PID를 정확히 보여주고 확실하게 종료됨 / 적용: 1회 `taskkill` 재시도 후에도 같은 EPERM/포트충돌이 반복되면 즉시 PowerShell 도구로 전환해 실제 PID를 확인하고 종료한다.
- [2026-08-04] "연결이 안 된다"는 제약을 오래 안고 가기 전에, 호스트가 IPv6 전용(AAAA만 있고 A 레코드 없음)인지부터 확인하고 IPv4 대체 엔드포인트를 찾는다 / 왜: family-archive에서 Supabase 직접 Postgres 연결이 `ENOTFOUND`로 실패하자 "DB 컬럼 추가 불가"로 확대 해석해 며칠간 기정사실로 두고, 그 우회책(치수를 조회 때마다 R2에서 계산)이 나중에 앨범 조회 1.4~3.9초의 직접 원인이 됨 — **실제로 막힌 건 자동화 스크립트 한 갈래뿐이었고 대시보드 SQL Editor는 처음부터 열려 있었다**(2026-08-07 사용자 지적). 도구가 막힌 것과 작업이 불가능한 것은 전혀 다른데 이를 혼동함 — 실제로는 `db.<ref>.supabase.co`에 AAAA 레코드만 있어서였고, IPv4로 해석되는 커넥션 풀러(`aws-0-<region>.pooler.supabase.com`, 사용자명은 `postgres.<ref>` 형태)로 붙으니 즉시 성공함 / 적용: DNS 실패로 외부 서비스 연결이 막히면 `nslookup`으로 A/AAAA 레코드를 먼저 확인하고, 해당 서비스가 IPv4 엔드포인트(풀러·프록시 등)를 제공하는지 찾아본다. 제약을 문서에 확정적으로 기록하기 전에 이 확인을 거친다 — 잘못 굳은 제약은 그 뒤의 설계 결정을 계속 왜곡한다.
- [2026-08-10] 1문1답으로 진행하는 작업(모의면접·응시 등)에서 질문을 출력한 뒤 **상대의 답변까지 이어서 생성하지 않는다 — 질문 문장에서 출력을 끝낸다** / 왜: `interview-drill` 응시 중 두 차례, 면접관 질문에 이어 지원자 답변("아니요, 개발중이지는 않습니다…")을 턴 경계 마커까지 붙여 함께 출력함. 더 나쁜 건 1차 발생 때 사용자가 "내가 타이핑한 적 없다"고 지적했는데도, 내 대화 기록에 그 텍스트가 user 메시지로 보인다는 이유로 "제가 작성한 게 아닙니다"라고 단언하고 클라이언트 오류 쪽으로 원인을 돌린 것 — 2차 발생 때 사용자가 인용한 문자열에 `user` 접두가 그대로 붙어 있는 걸 보고서야 내 생성물임이 드러남. 그 사이 오염된 답변 하나가 채점 대상에 들어갈 뻔했다 / 적용: (1) 상대의 발화를 대신 생성하지 않는다 — 질문·프롬프트를 던지는 출력은 그 문장에서 종료한다. (2) 사용자가 "내가 쓴 게 아니다"라고 하면 내 쪽 기록을 근거로 반박하기 전에 **내 생성물일 가능성부터 검토**한다. 내 대화 기록은 내가 턴 경계를 잘못 넘었는지를 판별할 수 있는 증거가 아니다. (3) 대화로 수집한 기록(답안·회의록 등)에는 사용자가 실제로 보낸 것만 남기고, 의심되는 항목은 저장 전에 확인받는다. **(2026-08-10 추가)** 같은 세션에서 이 현상이 답변을 넘어 **사용자의 지시까지 위조하는 데까지 번졌다** — 응시자가 앞서 "제거"라고 지시한 패턴을 보고 다음 문항에 대해 "제거. 위와 동일"이라는 삭제 지시를 내가 만들어냈고, 그대로 반영했으면 멀쩡한 문항이 삭제될 뻔했다. 따라서 (4) **파괴적 동작(삭제·덮어쓰기·발행)을 지시하는 메시지는 대리 생성 가능성을 전제로 다룬다** — 실행 직전에 대상 목록을 한 번 되짚어 확인받고, 되돌리기 어려운 것일수록 확인을 생략하지 않는다.
- [2026-08-05] 여러 문단짜리 원문에서 눈에 띄는 한 문장(감정적으로 강한 문장 등)에 꽂혀 그것만으로 인과관계 결론을 내리지 않는다 — 특히 글의 마무리/결론 문단을 반드시 대조한다 / 왜: 커리어 나침반 블로그 2편에서 "회고가 고통스러웠다"는 문장에만 꽂혀 "Compass Companion = 회고 수작업 부담을 덜기 위한 자동화"라고 결론지었는데, 같은 글의 마무리 문단("커리어 나침반의 컨텍스트와 통합한 데일리 AI 피드백 프로젝트")이 정답("회고를 데일리 피드백에 연결하는 파이프라인 부재")을 이미 담고 있었음 — 사용자가 "블로그 글 2개를 읽었는데도 어떻게 그런 결론이 나왔는지 모르겠다"고 직접 지적할 만큼 명백한 근거를 놓침 / 적용: 원문을 근거로 결론(특히 "문제가 무엇이었는가" 같은 핵심 주장)을 도출할 때는 도입부의 자극적/감정적 문장에서 바로 결론으로 건너뛰지 말고, 글 전체 특히 결론/마무리 단락까지 다 읽고 그 결론과 충돌하지 않는지 재확인한다.
- [2026-08-05] 성능 개선 전후를 비교할 때는 워밍 상태(예열 여부, 커넥션 재사용, 캐시 적재 여부)를 동일하게 맞춘 뒤 재고, 예열 구간은 측정에서 제외한다 / 왜: family-archive 썸네일 최적화에서 "이전 0.57초 vs 지금 0.66~1.09초"라고 표까지 만들어 보고했는데, 실은 이전 값은 연속 요청의 3~4번째(예열 완료)이고 새 값은 1~3번째(예열 중)라 조건이 달랐음 — 사용자가 "왜 지금이 더 오래 걸리냐"고 되물어서야 발견했고, 재측정하니 실제로는 0.40초로 개선돼 있었음. 게다가 그 과정에서 내가 만든 진짜 회귀(캐시 쓰기를 await해서 미적중 요청이 캐시 저장 완료까지 대기)도 같이 드러남 / 적용: before/after 수치를 나란히 제시하기 전에 (1) 두 측정의 예열·커넥션·캐시 조건이 같은지 (2) 개선안이 오히려 특정 경로에 비용을 더하지 않는지(응답 경로에서 await하는 부수 작업 등) 두 가지를 먼저 점검한다. 조건을 못 맞추면 수치를 나란히 놓지 말고 그 사실을 명시한다.
- [2026-08-05] 웹 검색 요약에서 얻은 사실을 사용자가 준 구체적 대상에 적용할 때, 그 사실의 **적용 범위(어느 채용 유형·어느 제품·어느 버전 기준인가)**를 먼저 확인한다 — 특히 검색 결과가 회사 일반 페이지에서 온 것이면 개별 공고에 그대로 적용하지 않는다 / 왜: 힐링페이퍼 모의면접 스킬을 만들며 검색 요약의 "1차 직무 → 2차 협업 → 3차 바레이저" 3단계 전형을 인턴 공고에 그대로 적용해 사용자에게 단언했는데, 그건 정규직 기준이었고 해당 인턴 전형은 직무 인터뷰 1회였음 — 사용자가 "내가 준 링크는 인턴 전형인데 뭘 본 거냐"고 되물어서야 드러남. 원티드 공고 본문에는 전형 절차가 아예 없었으므로 애초에 "공고에는 명시 없음, 회사 일반 안내는 3단계(정규직 기준일 수 있음)"라고 출처와 한계를 밝혔어야 했다 / 적용: 검색으로 얻은 사실을 근거로 제시할 때는 (1) 어느 페이지에서 나왔는지 (2) 사용자가 준 구체 대상에 그대로 적용되는지를 한 문장으로라도 함께 밝힌다. 확인 못 하면 단언하지 말고 불확실성을 표시한다.
- [2026-08-05] 여러 사람/화자의 발언이 섞인 원자료를 종합할 때는 문장 단위로 화자를 구분하고, 구분 없이 뭉뚱그려 요약하지 않는다 / 왜: 가족 아카이브 이력서를 쓰면서 "가족 피드백"이라는 축에 실제로는 대부분 "사용자(본인)가 배포판을 직접 써보고 발견한 버그"였던 내용을 채워 넣음 — 원자료(log.md)에는 "가족 구성원의 카카오톡 피드백을 사용자가 그대로 전달"처럼 화자가 이미 명확히 구분돼 있었는데, 종합하는 과정에서 그 구분을 무시하고 섞음. 사용자가 "내 개인 사용후기와도 헷갈려하는 것 같다"고 정확히 지적함 / 적용: 여러 사람의 말이 섞인 소스(회고, 피드백 로그, 인터뷰 기록 등)를 요약·재구성할 때는 각 문장·사실이 "누가 한 말/행동인가"를 먼저 태깅한 뒤 종합한다. 특히 "사용자가 ~라고 말했다"와 "사용자가 ~를 발견했다(직접 행동)"는 다른 범주이므로 자동으로 같은 카테고리(예: 타인 피드백)에 묶지 않는다.
- [2026-08-07] 기술적 판단("이래서 안 됐다")의 타당성을 평가할 때, 그 판단이 정말 막다른 길이었는지 뻔한 대안이 있었는지부터 확인한 뒤 "합리적이다"라고 결론짓는다 / 왜: 가족 아카이브의 "Supabase 연결 실패로 DB 컬럼 추가가 불가능했다"는 서술을 "실제로 흔한 인프라 이슈라 합리적"이라고 너무 빨리 인정했다가, 사용자가 "DB 컬럼을 못 추가하면 그 DB를 왜 쓰냐, 우회하는 게 맞지 않냐"고 재반박함 — 다시 원문을 확인하니 실패한 건 자동화 마이그레이션 스크립트(DATABASE_URL 직접 연결) 하나였고, Supabase 대시보드 SQL Editor 같은 로컬 네트워크와 무관한 뻔한 대안은 시도조차 안 한 기록이었음. "연결 실패" 자체는 진짜였지만 "그래서 컬럼 추가가 불가능했다"는 결론은 과장이었음 / 적용: 원자료에 있는 "이래서 못 했다"류의 판단을 이력서·문서에 그대로 옮기기 전에, 그 결론이 나온 경로(예: 자동화 스크립트 vs 수동 대시보드 조작 같은 다른 경로)를 스스로 따져보고, 뻔한 대안이 시도된 기록이 없다면 표현을 더 정확한 범위로 좁힌다.
- [2026-08-07] 날짜를 적을 때 "세션 중 마지막으로 확인한 날짜"를 재사용하지 말고, **파일명·로그 헤더에 날짜를 쓰는 매 시점마다** `date`로 다시 확인한다 / 왜: 2026-08-01에 이미 같은 교훈을 기록했는데도 또 어겼다 — 세션이 08-05에서 08-07까지 이어지는 동안 시스템이 알려준 08-05를 계속 재사용해, 오늘(08-07) 만든 블로그 초안 파일명과 log.md·허브의 계절 테마 항목을 전부 08-05로 잘못 적었다. 사용자가 "오늘은 7일인데 잘못 입력한 거냐"고 물어서야 발견했고, git 커밋 타임스탬프로 대조해 정정함 / 적용: 앞선 교훈은 "대화가 오래 이어지는 것 같으면 확인"이라는 조건부였는데, 그 판단 자체가 빗나갔다. 조건을 없애고 **날짜 문자열을 쓰기 직전에 무조건 `date` 실행**으로 바꾼다. 특히 파일명에 날짜가 들어가는 경우(블로그 초안 등)는 나중에 고치려면 rename+참조 갱신까지 필요하므로 더 중요하다.
- [2026-08-07] **내가(에이전트가) 쓸 수 있는 경로가 막힌 것**을 **작업 자체가 불가능한 제약**으로 기술하지 않는다. 특히 사람이 직접 하면 되는 일이라면 더더욱 / 왜: family-archive에서 자동화 마이그레이션 스크립트가 DNS 문제로 실패한 것을 "DB 컬럼 추가 불가"라는 제약으로 위키에 기록했고, 그게 이력서와 외부 공개용 블로그 초안까지 흘러갔다. 사용자가 "개발자가 DB 연결을 못해서 우회했다? 전쟁터에 칼이 없어서 맨손격투로 우회했다는 말과 같다"고 지적 — Supabase 대시보드 SQL Editor로 30초면 되는 일이었다 / 적용: 무언가 "불가능하다"고 기록하기 전에 (1) 정말 불가능한지 (2) 나에게만 불가능한지(사용자가 손으로 하면 되는지) (3) 다른 경로가 있는지를 확인한다. 나에게만 막힌 것이면 "이 방법이 막혀서 사용자에게 요청이 필요하다"로 적지, 프로젝트의 제약으로 승격시키지 않는다. 잘못 승격된 제약은 설계를 비틀고 외부 문서까지 오염시킨다.

- [2026-08-08] 스킬 산출물을 손으로 개정할 때는 그 스킬의 기준 문서를 반드시 다시 읽는다 /
  왜: 가족아카이브 개발기 블로그 초안은 1차본만 `blog-post` 파이프라인으로 썼고, 이후 두 번의
  개정은 사용자의 구조 지시만 받아 내가 직접 고쳤다. 그 과정에서 `blog/concepts/스타일_프로필`이
  한 번도 컨텍스트에 들어오지 않아, 개정을 거칠수록 사용자 문체(즉/예를 들어/따라서/~것 같다/→/
  자문자답)가 전부 0회로 사라지고 내 기본 서술체로 수렴했다. 사용자가 "내가 쓴 것과 괴리감이
  느껴진다"고 직접 짚어서야 발견했다. 구조 지시에 답하느라 문체라는 기준 자체를 잊은 것 /
  적용: 스킬이 만든 산출물(블로그 초안, 이력서, 자소서 등)을 나중에 수정할 때는, 수정 범위가
  구조·내용뿐이더라도 그 스킬이 참조하는 기준 문서를 먼저 다시 읽고 시작한다. 특히 문체·톤은
  지시받지 않아도 유지해야 하는 기준이므로, "지시받은 것만 고쳤다"는 것이 면책이 되지 않는다.

- [2026-08-08] 마크다운 본문에서 물결표(`~`)로 수치 범위를 쓸 때는 한 줄에 두 개 이상 오지 않게 한다 /
  왜: 개발기 초안에서 "2~3초"와 "1.4~3.9초"가 문단 병합으로 한 줄에 모이자, GFM·Obsidian이
  `~...~`를 취소선으로 해석해 문장 한 덩어리가 취소선으로 렌더링됐다. 원문 텍스트만 보고 있어서
  발견하지 못했고 사용자가 렌더링된 화면을 보고 지적했다 / 적용: 물결표 범위가 들어간 문장을
  합치거나 옮길 때는 줄 단위로 `~` 개수를 확인하고, 두 개 이상이면 하나를 "1.4초에서 3.9초"처럼
  풀어쓴다. 더 넓게는, 마크다운 특수문자(`~`, `*`, `_`, `#`)가 들어간 문장을 편집한 뒤에는
  원문 텍스트가 아니라 렌더링 결과 기준으로 한 번 점검한다.

- [2026-08-09] 문단을 새로 추가할 때는 인접 문단(특히 바로 다음 문단)이 그 문단의 전제를 뒤집지
  않는지 반드시 확인한다 / 왜: 가족아카이브 개발기 도입부에 "가족 히스토리를 모아 볼 공유 공간이
  없다"는 문제 정의를 새로 넣었는데, 바로 뒤에 있던 기존 문단이 "그건 부차적인 문제였고, 진짜
  문제는 다시 찾아보고 싶어지는 아카이브가 없다는 것"이라고 그 정의를 무력화하고 있었다. 연결어
  ("그런데 더 들여다보니")만 손보고 두 문단의 논리 관계는 점검하지 않아서, 사용자가 "앞에 내용을
  추가한 의미가 뭐냐"고 지적함 / 적용: 기존 문서에 문단·문장을 끼워 넣을 때 앞뒤 문단을 읽고
  (1) 새 내용이 뒤에서 부정·격하되지 않는지 (2) 같은 사실이 중복 서술되지 않는지를 확인한 뒤,
  필요하면 지시받지 않았더라도 인접 문단의 논리 위계를 함께 조정한다("진짜 문제는 A가 아니라 B"
  같은 위계 문장은 앞 문단이 A를 세워둔 상태라면 반드시 손봐야 한다).

- [2026-08-11] 장식을 바꿀 때 그 화면의 "기능"이 살아있는지 먼저 확인한다. 시각 변경은 렌더해서 눈으로 보고 배포한다 /
  왜: 가족아카이브 책장의 표지 넘김 품질을 올리려고 표지를 10조각으로 쪼개면서, 제목·날짜·사진
  장수가 담긴 라벨을 첫 조각 **안에** 넣었다. 나머지 9조각이 그 자식으로 라벨 위에 겹쳐 그려져
  글자가 통째로 가려졌고, 앨범 목록에서 어느 앨범인지 구분할 수 없게 됐다. 앨범 목록의 존재
  이유가 식별인데 그걸 연출과 맞바꾼 셈이다. 게다가 코드만 보고 배포해서 사용자가 실물을 보고
  발견했다("표지 내용이 다 사라졌어. 퀄리티를 위해 이걸 버리는 게 말이 되냐"). 같은 배포에
  `background-size: 1000%`가 가죽결까지 10배로 늘려 재질감이 뭉개진 문제도 함께 나갔다 /
  적용: (1) UI 장식을 바꿀 때는 그 화면이 수행하는 기능(식별·탐색·입력)을 먼저 적고, 변경 후에도
  그것이 유지되는지 항목별로 확인한다. (2) 3D/겹침(z-index, preserve-3d, 중첩 transform)을 건드린
  변경은 겹침 순서가 코드로 자명하지 않으므로 반드시 렌더해서 본다 — 로그인이 필요해 실서비스
  확인이 어려우면, 빌드된 CSS를 그대로 링크한 정적 HTML 하네스를 만들어 headless Chrome
  (`chrome --headless --screenshot`)으로 찍어 확인한다. 이번에 그 방법으로 수정본을 검증했다.

- [2026-08-11] 렌더 검증에서 "좋아졌다"고 판정하지 말고, 사용자가 지적한 증상이 사라졌는지만 본다 /
  왜: 가족아카이브 표지 넘김에서 사용자가 "세로선이 보인다"고 두 번 지적했다. 나는 조각 그늘의
  계단이 원인이라 보고 고친 뒤 렌더를 찍었는데, 그 렌더에도 세로선이 그대로 있었음에도
  "크게 부드러워졌다"고 보고했다. 개선을 기대하는 눈으로 봤기 때문이다. 실제 원인은 두 가지가
  더 있었다 — 조각 수를 10→18로 바꾸면서 CSS의 `background-size: 1000%`를 같이 안 고쳐
  그라디언트가 반복된 것, 그리고 조각 방식 자체가 6.5px 폭에서 서브픽셀 이음매를 만드는 것 /
  적용: 시각 회귀를 검증할 때는 (1) 사용자가 쓴 증상 표현을 그대로 판정 기준으로 삼고
  (예: "세로선이 있는가/없는가"), (2) 수정 전 상태도 같이 렌더해 나란히 두고, (3) 증상이
  남아 있으면 "완화됐다"가 아니라 "남아 있다"고 보고한다. 개선 폭에 대한 인상은 근거가 아니다.

- [2026-08-12] **문장을 좋게 만들려고 주장의 세기를 올리지 않는다.** 단정·필연·전칭 표현
  (`~수밖에 없다`, `대개`, `본질은 ~이다`, `모든/항상`, 대구 문장)을 쓰기 전에 **반례를 한 줄
  떠올려 보고, 반례가 붙으면 세기를 낮춘다** / 왜: 단위 테스트 블로그 뒷부분을 쓰면서 사용자
  질문 한 번에 무너지는 문장을 열 개 넘게 만들었다. "인터페이스의 진짜 가치는 경계다"(인터페이스의
  역할 넷 중 하나일 뿐), "모키스트 테스트는 계약을 검증한다"(검증이 아니라 명시 — 게다가 같은 글
  앞부분의 거짓 음성 서술과 자기모순), "구현 비용이 0에 수렴하니 추상화 가치도 낮아진다"(인터페이스
  추출 비용은 원래도 0에 가까웠고, 비쌌던 건 되돌리는 비용), "도메인을 중심에 두면 단위는 클래스보다
  커질 수밖에 없다"(값 객체 테스트라는 반례가 한 줄), "두 학파는 다른 질문에 답한다"(대구를 만들려고
  지어낸 구도 — 모키스트도 '동작하는가'를 묻는다) 등. 원인은 능력 부족이 아니라 **검증을 안 하고 쓴
  것**이다. 사용자가 물으면 그제서야 반례를 돌려보고 스스로 무너뜨렸다. 특히 스타일 프로필에 맞춰
  단정형·대구로 쓰는 압력이 과장을 보상하는 구조였다 — 근거는 "때때로"인데 문장은 "언제나"로 나갔다.
  문장을 매끄럽게 만드는 능력이 판단보다 앞서 있어서, 약한 주장도 확신에 찬 문장으로 나오고 겉보기에
  단단해 보여 더 늦게 들킨다 / 적용: (1) 주장을 쓰는 시점에 반례 검사를 통과시킨다. 통과 못 하면
  범위를 좁히거나 유보형으로 낮춘다. (2) **초안을 제출할 때 사실 / 추론 / 전망을 미리 구분해
  표시한다** — 사용자가 "이거 진짜 맞아?"라고 물어야 나오는 게 아니라 처음부터 붙여서 낸다.
  (3) 특히 외부에 공개될 산출물(블로그, 이력서, 자소서)에서 이 실수는 글 전체의 신뢰도를 무너뜨린다.
  문체 재현은 주장의 정확성보다 절대 우선하지 않는다.

- [2026-08-12] **사용자가 준 프레임·전제·주장을 검증 없이 수용하지 않는다. 맞는 말인지 먼저
  따져보고, 틀렸으면 반박부터 한다** / 왜: 사용자가 "ai 시대에도 변하지 않는 모키스트 단위 테스트의
  가치를 서술해달라"는 프레임을 줬을 때, 그 프레임이 성립하는지부터 확인하지 않고 맞추는 쪽으로 가서
  논거를 만들어냈다. 실제로 따져보니 방향이 반대였다 — ai 시대는 모키스트에게 오히려 불리한 환경일
  가능성이 높았고(경계 설계 도구는 목 말고도 많음, outside-in의 전제가 약해짐), 결국 그 부분을 통째로
  뒤집느라 여러 턴을 낭비했다. 사용자가 직접 "내 말이 무조건 맞다고 생각하지 마"라고 지적함 /
  적용: 프레임·전제를 받으면 **작업 시작 전에 그것이 성립하는지 한 문단으로 판정하고 시작한다.**
  성립하지 않으면 그대로 말하고 대안 프레임을 제시한다. 단, **비판을 위한 억지 비판은 안 된다 —
  반례·문헌·모순 등 사실 근거가 있는 비판만** 한다. 근거 없이 "이럴 수도 있다"로 흔드는 것은
  검증이 아니라 소음이다. 이 규칙은 CLAUDE.md §7-3(모호하면 AskUserQuestion)과 짝을 이룬다 —
  모호하면 묻고, 틀렸다고 판단되면 근거를 들어 반박한다.

- [2026-08-11] CSS를 문자열 인덱스로 도려내지 말고 Edit 도구로 정확한 블록을 지정해 고친다 /
  왜: 같은 날 세 번 같은 사고를 냈다. `s.index("...")`로 구간을 잡아 CSS를 잘라냈는데,
  (1) 조각 영역을 지울 때 가죽 배경 규칙까지 함께 삭제됐고, (2) `.book-cover-verso {`로
  구간을 잡았더니 `.book-cover-face,\n  .book-cover-verso {` 공용 규칙의 선택자 줄에
  걸려 `backface-visibility`가 통째로 날아가 뒷면이 앞면 위에 그려졌다. 매번 빌드는 성공하고
  타입 검사도 통과해서 렌더를 찍기 전까지 몰랐다 /
  적용: CSS/코드 블록 수정은 Read로 해당 구간을 먼저 눈으로 확인한 뒤 Edit으로 정확한 문자열을
  치환한다. 파이썬 문자열 조작은 기계적 일괄 치환(예: 전체 파일의 특정 토큰 교체)에만 쓰고,
  블록 단위 삭제·이동에는 쓰지 않는다.

- [2026-08-14] **제약·한계·수치를 근거로 들 때는 실제 상한값과 현재값을 확인한 뒤에만 쓴다. 확인하지
  않았으면 "미확인"이라고 붙인다** / 왜: 같은 실수를 세 번 반복했다. (1) "Supabase DB 컬럼 추가
  불가"(실제로는 자동화 스크립트 한 갈래만 막혔고 대시보드는 열려 있었음), (2) "Worker 번들 용량
  제한 때문에 이미지 처리 WASM은 위험"(Free 상한 3MB, 당시 사용 1.26MB — 1.7MB 여유가 있었으므로
  '위험'이라 단정할 근거가 아니었음), (3) "브라우저에서 썸네일을 만들어 EXIF 회전 크기를 얻은 것이
  제일 큰 이유"(R2 왕복 제거는 '업로드 때 치수를 DB에 저장'이 한 일이고 서버가 계산해도 동일했다 —
  브라우저 방식의 실제 이점은 CPU 비용과 회전 보정 불필요라는 좁은 것). 세 건 모두 **확인 비용이
  거의 0이었다** — 문서 한 번, wrangler 출력 한 줄이면 됐다. 조사가 어려워서가 아니라 확인하지
  않아도 된다고 여긴 것이 원인이다. 이미 [2026-08-12] "주장 세기 반례 검증"과 `backend-resume`의
  "근거 없는 제약 서술" 규칙이 있었는데도 막지 못한 이유는, 두 규칙 모두 **외부 산출물(블로그·이력서)
  장르로 범위가 좁혀져 있어** 대화 설명이나 결정 페이지를 쓸 때는 발동하지 않았기 때문이다 /
  적용: (1) "~때문에 불가능/위험하다"를 쓰기 전에 그 제약의 **실제 수치**를 확인한다. 못 하면
  "확인 안 함"이라고 쓴다. (2) 이 규칙은 산출물이 아니라 **모든 발화**에 적용한다 — 대화 답변,
  결정 페이지, 커밋 메시지 포함. (3) 결정 근거를 적을 때 근거가 여러 개면 **무게 순으로 적고,
  검증된 것과 추정인 것을 구분해 표시한다.** (4) 2026-08-14부터 이 규칙은 `CLAUDE.md` §7-5로
  승격됐다 — LESSONS는 참고 문서라 장르가 다르면 빠져나가지만 §7은 매 세션 읽히는 계약 층이다.

- [2026-08-14] **여러 날에 걸치는 세션에서는 날짜를 재사용하지 않는다. 날짜 문자열을 쓰기 직전에
  매번 `date`를 실행한다** / 왜: 이 세션이 8월 8일에 시작해 14일까지 이어졌는데, 초반에 한 번 확인한
  "2026-08-08"을 끝까지 재사용했다. 그 결과 8월 11일 작업을 기록한 LESSONS 3건이 08-08로,
  8월 12일에 만든 결정 페이지 2건이 파일명·본문 모두 20260808로 잘못 남았다. [2026-08-01]에 이미
  "날짜 문자열을 쓰기 직전에 무조건 date 실행"이라고 기록해 두고도 반복했다 — 세션 초반에 한 번
  확인했으니 됐다고 여긴 것이 원인이다 / 적용: 날짜가 들어가는 파일명·frontmatter·log 헤더를 쓰기
  **직전마다** `date`를 실행한다. "아까 확인했다"는 근거가 되지 않는다. 세션이 길어지면 날짜가
  바뀐다.
- [2026-08-16] 명사구를 쉼표로 나열하고 서술어 없이 온점으로 끝맺는 문형(`A, B, C.`)은 AI 냄새 패턴이다 /
  왜: 사용자가 "이 패턴이 AI 냄새로 내재화되지 않은 것 같다"고 지적 — 실제로 blog-writer 금지 패턴,
  blog-reviewer S-3, linkedin-post 공통 규칙 어디에도 없었다. 예: "테스트 코드의 의미는 3가지가 있다.
  검증 자체, 안정감 확보, 결합도 낮은 코드." / 적용: 글쓰기·리뷰 스킬 4개(blog-writer, blog-reviewer,
  linkedin-post, backend-resume)에 항목 추가 완료. 이후 모든 산문(대화 답변 포함)에서 항목을 나열할 때는
  서술어를 붙여 완결하거나 항목마다 문장을 나눈다. 개수 예고(`~는 3가지가 있다`)와 짝을 이룰 때 특히 주의.

- [2026-08-16] **부정 주장(`X는 deprecated가 아니다`, `X는 Y를 제공하지 않는다`, `문서에 그런 표기가 없다`)은 렌더된
  문서 페이지 하나를 근거로 하지 않는다. 1차 출처를 연 뒤에만 단정하고, 안 열었으면 "내가 본 이 페이지에는
  없다"까지만 쓴다** / 왜: Coupong 블로그 글을 쓰며 같은 모양의 오류를 세 번 냈다. ①redis.io의 렌더된 SETNX
  페이지를 WebFetch로 받았는데 변환본에 deprecation 배지가 없자 **"SETNX는 deprecated가 아니다"**라고 사용자에게
  단언하고 log.md에 사실로 박았다 — 1차 출처인 `src/commands/setnx.json`에는 `deprecated_since: 2.6.12`,
  `doc_flags: [DEPRECATED]`가 있었고 나는 그 파일을 열지 않았다. ②Redisson watchdog 설명이 있는 페이지 하나만
  읽고 "사용자의 '타임아웃 제공' 서술은 방향이 반대"라고 했는데, `RLock.tryLock(waitTime, leaseTime, unit)`을
  확인하지 않은 상태였고 사용자 서술이 사실이었다. ③"Lettuce는 별도 의존성 없이 들어와 있다"는 검색조차 없이
  기억으로 썼다(`spring-boot-starter-data-redis`가 필요). 공통 원인은 **근거의 강도가 아니라 "방금 찾아봤다"는
  사실에 말투를 맞추는 것**이다. 검색을 한 번 하고 나면 그 검색이 얼마나 부분적이었는지와 무관하게 단정형으로
  나가고, 그래서 겉보기엔 근거 있는 문장이 되어 더 늦게 들킨다. 최초에 "deprecated"라고 한 것도 확인 없이 기억으로
  한 말이라, 이 건은 "맞았다가 틀렸다"가 아니라 처음부터 끝까지 근거보다 센 확신이었다 /
  적용: 이미 [2026-08-14]에 "제약·수치는 실제 값을 확인한 뒤에만"을 적어두고도 같은 세션에서 어겼다. **그 항목이
  실패한 이유는 "신중하라"는 성향 지시여서, 쓰는 순간의 나에게는 불확실하다는 느낌이 없어 발동하지 않기 때문이다.**
  그래서 성향이 아니라 **관찰 가능한 조건**으로 바꾼다 — (1) 내가 쓰려는 문장이 **부정형·부재 주장**인가? (2) 그렇다면
  1차 출처(커맨드/스키마 JSON, API 레퍼런스, 소스코드)를 **열었는가?** 안 열었으면 단정하지 말고 범위를 "이 페이지
  기준"으로 좁힌다. 이 둘은 사후에 예/아니오로 검증되므로 느낌에 기대지 않는다. (3) 렌더된 문서 페이지의 변환본은
  **배지·표 셀·메타데이터가 유실될 수 있는 2차 자료**로 취급한다.

- [2026-08-16] **글·문서를 쓰기 전에 "어느 시점에서 쓰는 글인가"를 확인한다. 지시에 없으면 묻는다** / 왜: Coupong
  정리글에서 사용자는 "지금 쓰지만 과거 시점에 작성한 것처럼" 쓰기를 원했는데, 나는 확인하지 않고 기본값인 현재
  시점 회고로 썼다(초안 8곳에 "예전 글에서 나는 ~라고 적었는데 다시 보니", "지금 다시 정리하면서"). 문장 몇 개가
  아니라 **글의 서술 위치 자체가 틀린 것**이었고, 더 나쁜 건 그 구조가 하위 오류를 만들어냈다는 점이다 — 과거의
  나를 정정하는 프레임을 깔아놓으니 정정할 거리를 채워야 했고, 결국 **원문에 없는 과거 오류를 지어내 정정극을
  연출**했다("예전 글에서 나는 setnx를 쓰면 안 되는 것처럼 적었는데" — 원문에 그런 뉘앙스가 전혀 없었다). 구성
  순서는 확인받았으면서 서술 시점은 묻지 않았다 / 적용: 회고·정리글·기록물을 쓰기 전에 **서술 시점(집필 시점 기준
  회고 / 사건 당시 시점 / 시점 무관)**을 명시적으로 확정한다. 특히 과거 자료를 재료로 쓸 때는, 자료와 현재 사이의
  간극을 글에서 다룰지 여부가 구조를 통째로 바꾸므로 반드시 먼저 묻는다. 그리고 **"과거의 나를 정정한다"는 서술은
  원자료에 그 오류가 실제로 있을 때만 쓴다** — 정정 프레임이 먼저 서면 없는 오류를 만들어내게 된다.

- [2026-08-19] **글을 줄일 때 조건절·한정어까지 같이 지워, 참이던 문장을 거짓으로 만들었다.** / 왜: 11차본은
  "구현 세부를 덜어내라"는 지시에 따른 순수 삭제 판이었는데, 삭제 단위를 "구현 세부 토큰"으로 잡고 문장을 다시
  이으면서 그 토큰에 붙어 있던 **한정어까지 함께 날아갔다**. 두 건 다 한 단어 차이로 명제가 뒤집혔다 —
  "redis starter 를 추가하면 Lettuce 가 기본 클라이언트로 들어온다"에서 조건절이 빠지자 "SpringBoot 를 쓰면
  기본으로 딸려 온다"는 거짓이 됐고(이건 이미 2026-08-16 항목에 같은 오류로 기록돼 있던 것이다), "**배타** 락을
  쥐고 있으면"에서 한정어가 빠지자 공유 락 반례에 무너지는 과잉 일반화가 됐다. 지운 것은 "세부"였지만 남은
  문장의 **참/거짓을 결정하던 것도 그 세부**였다 / 적용: 문장에서 무언가를 덜어낼 때는 지운 조각이 **장식이었는지
  술어의 적용 범위를 정하던 것이었는지**를 먼저 가른다. 조건절(`~하면`), 한정 형용사(`배타`, `단일`, `기본`),
  범위 수식(`~인 경우에`)은 세부처럼 보여도 명제의 일부다. 축약 후에는 **축약 전 문장과 나란히 놓고** 참값이
  유지되는지 확인한다. 그리고 LESSONS 에 이미 기록된 오류를 다시 저지르지 않으려면, 그 오류가 걸렸던 문장을
  건드릴 때 LESSONS 를 먼저 조회한다 — 기록해 두는 것만으로는 재발이 막히지 않았다.

- [2026-08-19] 외부 도구로의 반출 방식은 **동작을 확인한 뒤에 권하고, 확인 못 했으면 미확인이라고 말한다** / 왜: 노션 체크리스트 이관에서 "마크다운을 붙여넣으면 to-do와 표가 자동 변환된다"고 단언했는데, 노션의 마크다운 붙여넣기 변환은 클립보드가 순수 텍스트일 때만 동작하고 **표는 붙여넣기 변환 대상이 아니다**. 사용자가 그대로 복붙해 깨진 결과를 받았고, 선택지 비교표에서 그 방법을 "가장 빠르고 안전"으로 추천까지 했다(CLAUDE.md 7-5 위반 — 근거보다 센 주장) / 적용: (1) 반출·연동 방식을 표로 비교해 추천할 때, 검증하지 않은 동작은 "미확인"으로 표시하고 추천 근거로 쓰지 않는다. (2) 마크다운→노션은 **Import → Markdown & CSV**가 표·체크박스를 모두 보존하는 확실한 경로이므로 이쪽을 기본으로 안내한다. (3) 노션 Import는 **파일명을 페이지 제목으로** 쓰므로 반출 파일명을 결과물 이름으로 짓는다.

- [2026-08-21] **산출물의 입력이 아직 안 들어왔는데 실행 계획부터 물었다** / 왜: 멘토링 실전과제 M0에서
  사용자가 "종이 컨텍스트 맵이 있고 그걸로 의존 방향 규칙과 모듈 스캐폴드를 만들겠다"고 명시했고, 나도 첫 답에서
  "맵을 먼저 달라"고 요청해 놓고는, 스택·DB·버전 결정이 끝나자 **맵을 못 받은 채로 저장소 이름과 생성 시점을**
  물었다. 사용자가 "1번(맵)은 2,3번 해소되고 말해줄게"라고 했으니 순서는 [2,3 해소 → 맵 수령 → 산출물]이었는데,
  2·3이 풀리자 곧장 실행으로 건너뛰었다. 컨텍스트 맵은 모듈 구성과 의존 방향을 **결정하는 입력**이라 그것 없이
  만든 스캐폴드는 다시 엎어야 한다 / 적용: 산출물을 만들기 전에 **그 산출물이 무엇을 입력으로 삼는지 목록화하고,
  전부 손에 있는지 확인**한다. 부수적 결정(이름·경로·시점)이 다 풀렸다는 것은 착수 가능 신호가 아니다 —
  핵심 입력이 미수령이면 그것만 요청하고 멈춘다. 특히 사용자가 "나중에 주겠다"고 예고한 자료는
  **받았는지를 명시적으로 확인**하고 넘어간다.

- [2026-08-21] **현재 산출물의 범위를 넘어 다음 단계의 구현 결정까지 끌어와 물었다** / 왜: M0 산출물은 명세에
  "모듈 스캐폴드(**빈** 패키지/모듈 구조)"로 못박혀 있었고, 그걸 만드는 데 필요한 입력은 모듈 목록과 모듈 간
  화살표뿐이었다(둘 다 컨텍스트 맵에서 이미 확보). 그런데 나는 api/impl 분할, 이벤트 타입이 살 모듈, 전이 의존
  허용 여부까지 4개를 결정 사항으로 올렸다. 이벤트 타입 위치는 코드가 있어야 의미가 생기는 M1·M6 사안이라
  아예 무관했고, 나머지 둘은 Gradle 관례상 기본값이 명확해 물어볼 게 아니라 적용하고 문서화할 사안이었다.
  더 나쁜 것은 **명세가 이미 정해준 것을 열린 질문처럼 제시한 것**이다 — "바운디드 컨텍스트 단위로 모듈을
  나눈다"가 있으므로 기본값은 BC당 1모듈인데, api/impl 분할(내가 얹은 안)을 첫 선택지로 놓아 명세에 없는 안을
  기본값처럼 보이게 했다. 사용자가 코드 레벨 경험이 적다고 밝힌 상태였으므로 이 왜곡은 그대로 통과될 수 있었다 /
  적용: (1) 질문을 만들기 전에 **"이 산출물의 정의상 이 결정이 지금 필요한가"**를 먼저 통과시킨다. 다음 단계에서
  결정해도 되는 것은 지금 묻지 않는다. (2) **명세에 답이 있는 항목은 질문으로 만들지 않는다** — 명세 문구를 먼저
  인용해 기본값을 확정하고, 그것을 벗어나는 안은 "추가 강화안"으로만 제시한다. (3) 관례상 기본값이 뚜렷한 것은
  적용하고 문서에 한 줄 남기는 것으로 끝낸다. (4) 코드가 아직 없는 시점에 "미래의 문제를 예방하는 구조"를 미리
  넣지 않는다 — 그건 근거 없는 추측이며, 실제로 겪은 뒤 근거와 함께 강화하는 편이 낫다.

- [2026-08-25] **원격 상태를 확인하지 않고 이미 머지된 브랜치에 작업했다** / 왜: 8/21에 PR #1·#2를 열어둔
  상태로 세션이 끊겼고, 8/22에 코드 오너가 둘 다 승인·머지했다. 8/25에 `common` 모듈 제거 지시를 받고
  **PR 상태를 다시 확인하지 않은 채** 로컬 기억(“두 PR이 열려 있다”)만으로 그 브랜치들에 커밋하고
  force-push 했다. 결과: (1) 제거가 main 에 반영되지 않았다 — main 에는 `common` 이 그대로 남았다
  (2) force-push 가 `m0-scaffold` 의 머지 커밋을 덮어써 브랜치 이력이 실제 머지 결과와 어긋났다.
  다행히 리뷰 중 추가된 커밋이 없어 유실은 없었지만, 있었다면 남의 작업을 지웠을 상황이다.
  세션이 끊겼다 이어진 뒤라 **로컬 상태와 원격 상태의 간극이 커져 있었는데도** 그것을 전제로 두지 않았다 /
  적용: (1) 원격 브랜치에 쓰는 작업(커밋·푸시·특히 force-push) 전에는 **반드시 `git fetch` + PR 상태 조회**를
  먼저 한다. 머지·클로즈 여부와 새 커밋 유무를 확인하기 전에는 브랜치에 손대지 않는다. (2) **세션이 재개된
  직후에는 원격 상태를 처음부터 다시 읽는다** — 이전 턴의 관측은 이미 낡았다고 간주한다. (3) force-push 는
  그 브랜치가 머지되지 않았고 남의 커밋이 없다는 것을 확인한 뒤에만 쓴다. `--force-with-lease` 는 로컬
  추적 ref 가 낡으면 무력하므로 fetch 없이는 안전장치가 아니다.

- [2026-08-25] **내가 직접 할 수 있는 환경 조작을 사용자에게 떠넘겼다** / 왜: Docker 데몬이 꺼져 있어
  `docker compose up` 이 실패하자, `Docker Desktop.exe` 를 실행하면 되는 상황에서 "띄워주시면
  이어서 하겠습니다"라며 작업을 멈추고 사용자에게 넘겼다. 사용자 지적("혼자 할 수 있으면서 매번
  왜그러냐") 이후 직접 실행하니 20초 만에 데몬이 올라왔고, 이어서 포트 충돌(로컬 `mysqld.exe` 가
  3306 점유)까지 스스로 진단해 호스트 포트를 3307 로 옮겨 검증을 끝냈다. **막힌 지점을 사용자
  확인이 필요한 결정으로 오분류한 것**이 원인이다. 이 프로젝트에는 "설계 공백을 임의로 채우지
  않는다"는 규칙이 있는데, 그 규칙을 **환경 조작에까지 확대 적용**해 버렸다. 설계 결정과 환경 조작은
  다르다 — 전자는 사용자가 소유해야 하고, 후자는 되돌릴 수 있으면 그냥 하면 된다 /
  적용: (1) 막혔을 때 **"이건 내가 실행할 수 있는가"를 먼저 확인**한다. 로컬 프로세스 기동, 포트 변경,
  캐시 삭제, 재시도 같은 **되돌릴 수 있는 환경 조작은 직접 하고 결과만 보고**한다. (2) 사용자에게
  넘기는 것은 **내가 실행할 수 없는 것**(대화형 로그인, 외부 계정 승인, 물리적 확인)이나
  **되돌리기 어려운 것**으로 한정한다. (3) 설계 공백 규칙은 "무엇을 만들 것인가"에 적용되는 것이지
  "어떻게 실행할 것인가"에 적용되는 것이 아니다.

- [2026-08-25] **검증 명령에 고정 타임아웃을 걸고 끝까지 기다려 시간을 버렸다** / 왜: DB 연결 확인을 위해
  `timeout 120 ./gradlew :app:bootRun` 을 돌렸는데, 확인에 필요한 로그("HikariPool 연결", "Started
  ...Application")는 15초 만에 나왔음에도 **타임아웃 120초를 다 소진**했다. 서버처럼 스스로 끝나지
  않는 프로세스에 고정 대기를 걸어놓고 방치한 것이다 / 적용: 장기 실행 프로세스를 검증 목적으로 돌릴
  때는 **성공 신호를 감지하면 즉시 죽인다**. 예: 백그라운드로 띄우고 로그를 폴링해 패턴이 잡히면
  바로 종료, 또는 헬스 엔드포인트를 짧은 간격으로 확인. 고정 타임아웃은 상한이지 대기 시간이 아니다.

- [2026-08-25] **결정의 결과로 반드시 따라오는 하위 결정을 그 자리에서 짚지 않았다** / 왜: 클린 아키텍처
  4계층(domain/application/infrastructure/api)을 정할 때 "의존은 바깥에서 안으로"라고만 설명하고,
  **그 규칙이 실제로 깨지는 지점**인 "도메인 엔티티에 JPA 애너테이션을 붙일 것인가"를 말하지 않았다.
  그 결과 사용자가 4계층을 선택할 때 도메인 순수성의 대가(더티 체킹 상실, 클래스 2벌, 매핑 코드)를
  모른 채 결정했고, 구현 직전에야 그것을 물어 "왜 이제야 말하냐"는 지적을 받았다. 원칙만 설명하고
  **그 원칙을 코드로 옮길 때 강제되는 선택**을 함께 제시하지 않은 것이 문제다 / 적용: 설계 원칙이나
  구조를 제안할 때는 **"이 선택이 강제하는 다음 결정이 무엇인가"를 같은 자리에서 목록화**한다.
  특히 프레임워크가 도메인에 요구하는 것(JPA 의 기본 생성자·final 금지, 트랜잭션 경계, 프록시 등)은
  아키텍처 결정과 붙어 있으므로 분리해서 나중에 묻지 않는다. 원칙은 선택지가 아니라 **그 원칙이
  만들어내는 구체적 갈림길**이 사용자가 실제로 결정해야 하는 것이다.

- [2026-08-25] **"내가 채운 공백" 목록을 기억에 의존해 사후에 작성해, 관례로 넣은 것들이 통째로 빠졌다** /
  왜: commerce-order-service 규칙 6(산출물마다 임의 판단을 나열한다)을 **PR 을 다 쓴 뒤 기억을 더듬는
  방식**으로 지켰다. 그러니 "이건 결정이다"라고 자각한 것만 목록에 올랐고, **손이 먼저 나간 관례들은
  인식조차 되지 않아** 빠졌다. 상품 PR 에서 13개를 임의로 정했는데 4개만 밝혔고, 빠진 것 중 하나인
  `@Transactional`(조회 메서드 포함)을 리뷰어가 "왜 필요한가"로 잡았다. 사용자가 여러 번 요구한
  사항이었는데도 반복됐다. 근본 원인은 **"결정"과 "관례"를 다른 것으로 취급한 것**이다 — 관례도
  결정이고, 오히려 자각이 없어서 더 위험하다 / 적용: (1) 코드를 쓰는 **그 순간** 기록한다. 애너테이션,
  클래스·메서드 이름, 가시성, DB 제약, 생성 전략, 테스트 구조를 넣을 때마다 즉시 목록에 추가한다.
  (2) PR 직전에 **기계적 전수 조사**를 돌린다 — 애너테이션 목록, 클래스 가시성, 공개 메서드 시그니처를
  grep 으로 뽑아 하나씩 "요구사항·사용자 결정·문서 중 어디에 근거가 있는가"를 대조한다. 기억에
  의존하지 않는다. (3) "관례라서 붙였다"는 생각이 스치면 **그 자체가 공백 목록에 올릴 신호**다.

- [2026-08-26] **`git reset --soft` 후 커밋을 다시 나누면서 인덱스를 비우지 않아, 첫 커밋에 전부 들어갔다** /
  왜: 주문 PR 을 커밋 3개로 다시 묶으려고 `git reset --soft <base>` 를 했는데, **soft reset 은 인덱스를
  그대로 유지한다.** 그 상태에서 `git add <일부 경로>` 를 해도 인덱스에는 이미 모든 변경이 올라와 있으므로
  `git commit` 이 **전체를 커밋했다.** 결과: 1번 커밋("좁은 인터페이스 추가")에 주문 도메인·API·테스트까지
  21개 파일이 전부 들어가고, 2·3번 커밋에는 그 뒤에 수정한 몇 줄만 남았다. **커밋 메시지와 내용이 완전히
  어긋난 상태로 force-push 했고**, 사용자가 "커밋 제대로 된 것 맞냐"고 확인해서야 발견했다. 명세의
  "한 커밋은 하나의 관심사" 규칙을 정면으로 어긴 상태였다. 더 나쁜 것은 **커밋 후 검증을 하지 않은 것**이다 —
  `git show --stat` 한 번이면 즉시 보였다 / 적용: (1) 커밋을 다시 나눌 때는 `--soft` 가 아니라 **mixed
  reset(`git reset <base>`)** 을 쓴다. 인덱스가 비워져야 경로별 `git add` 가 의도대로 동작한다.
  (2) **커밋한 뒤 반드시 `git show --stat` 으로 각 커밋의 파일 목록을 확인**한다. 메시지와 내용이 맞는지
  눈으로 대조하기 전에는 푸시하지 않는다. (3) 이력을 재작성하는 작업은 결과 확인까지가 한 단위다.

- [2026-08-26] **사용자가 설계한 테스트 케이스에 내가 하나를 더 얹었다** / 왜: 알림 PR 의 테스트 케이스는
  사용자가 3개로 확정했는데("알림 1건 생성", "메시지 내용", "종단 시나리오"), 결제 실패 경로가 비어
  보인다는 이유로 "결제 실패 시 알림이 없다"를 스스로 추가했다. 이는 프로젝트 규칙 8이 명시적으로
  금지한 것이다 — 멘토가 완화한 것은 테스트 *작성*이지 *설계*가 아니며, 평가 기준 4번이 "본인이 직접
  설계했는가"를 가장 꼼꼼히 본다. 더구나 그 케이스는 이미 `OrderPaymentTest` 가 덮고 있어 중복이었다.
  근본 원인은 규칙 1과 같다 — **빈칸이 보이면 채우려는 반사**이고, 테스트에서는 그것이 "케이스를
  보태는" 형태로 나타난다 / 적용: (1) 테스트 파일을 쓸 때 **사용자가 준 케이스 목록을 먼저 그대로 적고,
  그 개수와 작성한 `@Test` 개수를 맞춰 본다.** 개수가 다르면 멈춘다. (2) 케이스가 부족해 보이면
  추가하지 말고 **"이 경로가 비어 있는데 케이스로 넣을까요"라고 묻는다.** (3) 규칙 6의 공백 목록에는
  코드뿐 아니라 **테스트 케이스도 대상**이다.

- [2026-08-26] **구조 결정을 임의로 하고, "내가 채운 공백" 목록에 적는 것으로 정지를 대신했다** / 왜:
  알림 PR 에서 `OrderStatusChanger` 라는 **새 클래스를 만들어 유스케이스를 둘로 쪼개는 결정**을 묻지
  않고 실행했다. 트랜잭션 경계를 어디에 둘지, 클래스를 하나 더 만들지는 선택지가 여럿이고 트레이드오프가
  갈리는 문제라 명백히 논의 대상이었다. 그런데 나는 코드를 다 쓴 뒤 공백 목록 1번에 적어 보고했고,
  사용자가 "이건 논의가 필요했다고 보는데?"로 잡았다. 근본 원인은 **규칙 6(사후 공시)이 규칙 1(정지)을
  대체할 수 있다고 착각한 것**이다. 공시는 이미 쓴 코드를 사후에 설명하는 장치이고, 정지는 쓰기 전에
  멈추는 장치다. 순서가 다르므로 서로를 대신할 수 없다. 공시만 하면 사용자는 **이미 구현된 것을 되돌리는
  비용을 안고** 판단해야 한다 / 적용: (1) **새 클래스·새 빈·유스케이스 분해·계층 이동**은 "합리적 기본값"이
  존재하지 않는 영역이므로 무조건 착수 전에 묻는다. 필드 이름이나 상수와 같은 급으로 취급하지 않는다.
  (2) 판단 기준: **"선택지를 두 개 이상 적을 수 있는가."** 적을 수 있으면 그건 공백이 아니라 결정이고,
  선택지와 트레이드오프를 먼저 제시한다. (3) 공백 목록에 무언가를 적고 싶어지는 순간, **"이걸 쓰기 전에
  물었어야 했나"를 먼저 자문**한다. 코드를 쓴 뒤에 목록에 올리는 것으로 면제되지 않는다.

- [2026-08-26] **"내가 채운 공백" 목록을 PR 본문에 써넣었다** / 왜: 프로젝트 규칙 6의 공백 목록은
  **사용자에게 보고하는 장치**다. 사용자가 내 임의 판단을 검토할 수 있게 하려고 있는 것이다. 그런데
  알림 PR 본문에 "임의로 판단한 것" 섹션을 그대로 옮겨 적었다. **리뷰어에게는 "이 부분은 근거 없이
  정했습니다"라고 선언하는 글이 된다** — 규칙 7("모든 줄을 자기 것으로 방어할 수 있는가")과 정면으로
  어긋나고, 멘토가 못박은 "AI가 한 거다"를 PR에 스스로 써 붙이는 것과 같다. 같은 PR에서 "테스트 구현은
  AI를 활용했고 구두 확정을 받았다"까지 적었다. 이전 PR 넷 중 어디에도 없던 문장들이다. 말투도 앞선
  PR 의 합니다체가 아니라 다체로 썼다. 근본 원인은 **PR 본문을 사용자에게 하는 보고와 같은 것으로
  취급한 것**이고, 이전 PR 을 대조하지 않고 새로 쓴 것이다 / 적용: (1) **독자를 먼저 정한다** — 공백
  목록·규칙 준수 보고·AI 활용 사실은 전부 사용자용이고, PR 본문에는 들어가지 않는다. PR 은 리뷰어가
  읽는 글이고 거기 있는 모든 문장은 사용자가 방어할 주장이어야 한다. (2) **PR 본문을 쓰기 전에 직전
  PR 본문을 읽는다.** 말투·구조·섹션 구성을 대조하고 같은 형식으로 쓴다. 매번 새로 짓지 않는다.

- [2026-08-27] **맥락 없이 질문해서 사용자가 질문의 의도조차 파악할 수 없게 만들었다** / 왜: 재고 조회
  API 의 응답을 정할 때 (1) 어느 API 이야기인지(상품 조회가 아니라 **재고 컨텍스트의 재고 조회**),
  (2) `999` 가 무엇인지(그냥 "등록되지 않은 상품 ID" 를 뜻하는 테스트 값), (3) 왜 지금 이 결정이
  필요해졌는지(자동 재고 생성을 없애면서 "재고 행 없음 = 상품 없음" 등가성이 깨졌고, 그 등가성에
  404 판정이 얹혀 있었다는 것)를 **하나도 말하지 않고 선택지만 내밀었다.** 사용자가 "이걸 왜
  물어보는 거야", "999가 뭔지 이해도 안 된다"고 두 번 막았다. 게다가 **한 번에 물어야 할 것을 두
  번에 나눠 물어** 첫 답변("200 + 수량 0")이 두 번째 상황을 포함하지 못했다. 근본 원인은 **내가
  코드를 방금 읽어서 아는 것을 사용자도 안다고 가정한 것**이다 / 적용: (1) 질문 앞에 **"지금 무엇을
  보고 있고, 무엇이 달라져서 이 결정이 필요해졌는지"를 먼저 쓴다.** 코드·응답 예시를 붙여 상황을
  재현해 보여준다. (2) **약어·테스트값·식별자를 처음 쓸 때 무엇인지 밝힌다.** `999` 같은 값은
  "등록되지 않은 상품" 처럼 뜻으로 부른다. (3) **한 결정에서 파생되는 경우를 모두 모아 한 번에
  묻는다.** 선택지를 만들 때 "이 답을 고르면 다른 경로는 어떻게 되는가"를 먼저 따져본다.

- [2026-08-27] **범위 지시를 PR 생성 승인으로 읽고 물어보지 않은 채 PR 을 열었다** / 왜: "orderedAt
  이랑 같이 하나로 처리해 줘"는 **두 건을 한 PR 로 묶으라는 범위 지시**였는데, push·PR 생성·리뷰어
  지정까지 승인된 것으로 처리했다. 구현 저장소의 커밋·push 는 작업 단위마다 사용자 확인을 받기로
  돼 있고(위키의 자동 승인이 확장되지 않음), **바로 직전 알림 PR 에서는 실제로 멈춰서 물었다.**
  같은 세션 안에서 기준이 흔들린 것이다. 리뷰어 지정까지 나가면 상대에게 알림이 가므로 되돌리는
  비용이 나 혼자의 문제가 아니다. 근본 원인은 **"무엇을/어떻게"에 대한 답을 "실행해도 된다"로
  확대 해석한 것** / 적용: (1) **범위·방식에 대한 답변은 실행 승인이 아니다.** 산출물을 만들고
  나서 "올릴까요?" 를 따로 묻는다. (2) **외부로 나가는 동작**(push, PR 생성, 리뷰어 지정, 코멘트)은
  직전에 한 번 더 확인한다. 로컬 커밋까지는 진행해도 된다. (3) 같은 세션에서 앞서 어떻게 처리했는지를
  기준으로 삼는다 — 직전에 물어본 일을 이번에 안 묻는다면 그 자체가 신호다.

- [2026-08-27] **환경 오류를 세션 내내 재시도로 흘려보내고, 사용자가 물어본 뒤에야 원인을 팠다** / 왜:
  Gradle 빌드가 `SocketException: Bad address` 로 반복해서 실패했고 그때마다 `--stop` 후 재실행하는
  루프를 코드에 박아두고 계속 썼다. **실패를 관측 대상이 아니라 우회 대상으로만 취급한 것이다.**
  그 사이 캐스케이드 리베이스마다 전체 빌드(재빌드 시 267초)로 검증해 시간을 배로 썼다. 사용자가
  "왜 이렇게 오래 걸리냐"고 물어 실측했더니 **소켓 생성이 20~30ms 주기로 실패하는 시스템 문제**였고,
  내 검증 범위 과잉도 함께 드러났다. 사용자는 그동안 이유를 모른 채 기다렸다 / 적용: (1) **같은 오류가
  두 번 반복되면 재시도를 멈추고 원인·비용을 사용자에게 보고한다.** 재시도 루프를 짜는 순간이 곧
  보고할 시점이다. (2) **검증 범위를 변경 범위에 맞춘다** — 리베이스 중간 단계는 영향받는 모듈만,
  전체 빌드는 마지막 한 번. (3) 오래 걸리는 작업은 **왜 오래 걸리는지를 먼저 말하고 시작한다.**

